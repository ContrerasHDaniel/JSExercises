/**
 * Check if variable is array type
 * @param {any} input The object to verify
 * @return {boolean}   is an array
 */
function isArray(input) {
  return Array.isArray(input);
}

module.exports = isArray;